# Test environment configuration - SAFE TO COMMIT
# This file contains only test-specific values, no production secrets

###> symfony/framework-bundle ###
KERNEL_CLASS='App\Kernel'
APP_ENV=test
APP_SECRET='$ecretf0rt3st'  # Test-only secret, safe to commit
APP_DEBUG=false
###< symfony/framework-bundle ###

###> Database Configuration (Test) ###
# Test database settings - safe to commit
DB_HOST=db                           # Use Docker service name
DB_PORT=3306                         
DB_NAME=fund_transfer_db_test        # Separate test database
DB_USER=root                         
DB_PASSWORD=password                 # Default Docker password, safe for tests
DB_VERSION=8.0                       

DATABASE_URL="mysql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?serverVersion=${DB_VERSION}&charset=utf8mb4"
###< Database Configuration (Test) ###

###> Redis Configuration (Test) ###
# Test Redis settings - safe to commit  
REDIS_HOST=redis                     # Use Docker service name
REDIS_PORT=6379                      
REDIS_URL="redis://${REDIS_HOST}:${REDIS_PORT}"
REDIS_CACHE_PREFIX="test_fund_transfer_"  # Different prefix for tests
###< Redis Configuration (Test) ###

###> Test-Specific Configuration ###
# These values are for testing only - safe to commit
DAILY_TRANSFER_LIMIT=50000.00        # Lower limits for faster tests
SINGLE_TRANSFER_LIMIT=25000.00       
MINIMUM_TRANSFER_AMOUNT=0.01         
API_RATE_LIMIT_PER_MINUTE=1000       # Higher limit for test performance
TRANSACTION_TIMEOUT_MINUTES=1        # Faster timeout for tests

# Test logging
LOG_LEVEL=error                      # Reduce noise in test logs
LOG_CHANNEL=test
###< Test-Specific Configuration ###
